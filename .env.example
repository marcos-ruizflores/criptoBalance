# Copy to .env and fill in your values.

MONGO_URI=mongodb://localhost:27017/criptoBalance
MONGO_DB_NAME=criptoBalance

# Optional: email alerts (for Gmail use an app password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=
ALERT_EMAIL_TO=
