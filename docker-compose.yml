version: "3.8"

services:
  mongo:
    image: mongo:7
    container_name: cripto-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    command: ["--bind_ip_all"]

volumes:
  mongo_data:
